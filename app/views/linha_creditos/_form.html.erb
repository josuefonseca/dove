<%= form_with(model: linha_credito) do |form| %>
  <% if linha_credito.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(linha_credito.errors.count, "error") %> prohibited this linha_credito from being saved:</h2>

      <ul>
        <% linha_credito.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
